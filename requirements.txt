# FinHOLLY 전체 프로젝트 의존성 (app/ + scripts/ + demo/ 개발 환경 기준)
# 버전은 개발 PC(Python 3.11.4)에 실제 설치된 버전으로 고정(pin)함.
#
# 배포용 최소 의존성만 필요하면 FinHOLLY/requirements.txt (온프레미스 데모 서버용)
# 태국어/미얀마어 분절기 폐쇄망 설치용 wheel 목록은 requirements-offline.txt 참고.

# --- 번역 엔진 / 모델 ---
ctranslate2==4.8.2
transformers==4.57.6
sentencepiece==0.2.2
huggingface_hub==0.36.2
torch==2.13.0

# --- 웹 서버 (데모) ---
flask==3.1.3

# --- 데이터 처리 ---
pandas==3.0.5
numpy==1.26.4

# --- 평가 (COMET-Kiwi, LaBSE, sacreBLEU) ---
unbabel-comet==2.2.7
sacrebleu==2.6.0

# --- 언어별 분절/전처리 ---
pythainlp==5.3.7
pyidaungsu==0.1.4
myanmartools==1.2.1
fasttext-wheel==0.9.2
pybind11==3.1.0
emoji==2.15.0
python-crfsuite==0.9.12

# --- 외부 API 연동 ---
deepl==1.32.0
requests==2.33.1
tenacity==9.1.4
beautifulsoup4==4.15.0

# --- 환경설정 ---
python-dotenv==1.0.1

# --- Google Colab 파인튜닝 스크립트 전용 (scripts/colab_finetune_*.py) ---
# google.colab은 Colab 노트북 환경에 기본 내장되어 있어 pip 설치 대상이 아님.
# peft만 Colab 노트북에서 별도로 pip install 필요.
peft
